interface:
  display_name: "Speak Human"
  short_description: "Keep generated UI copy clear, factual, and user-facing"
  default_prompt: "Use $speak-human to review this interface copy, preserve verified meaning and real actions, and remove internal development language."

policy:
  allow_implicit_invocation: true
